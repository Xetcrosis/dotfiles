
# Created by newuser for 5.9

eval "$(starship init zsh)"

eval "$(zoxide init zsh)"

setopt auto_cd

alias ls='lsd'
alias l='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias lt='ls --tree'
alias v='nvim'
